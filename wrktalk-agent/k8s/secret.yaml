apiVersion: v1
kind: Secret
metadata:
  name: wrktalk-agent-secret
  namespace: default
type: Opaque
stringData:
  # Replace with your actual ArgoCD auth token
  # Get token with: argocd account generate-token --account <account-name>
  ARGOCD_AUTH_TOKEN: "your-argocd-token-here"
